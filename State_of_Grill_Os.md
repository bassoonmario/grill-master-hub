# Стан Grill_Os (AGENT_HANDOFF_8)

- **Дата останнього запису:** 2026-06-27
- **Статус проекту:** Активна розробка. Дедлайн — кінець тижня (міграція на новий сервер).
- **Поточний фокус:** Система алярмів і поповнення кейс-компонентів повністю реалізована. Логування списань і AdminSystem завершено. Залишок беклогу і підготовка до міграції.
loot_box архітектура реалізована. AdminWarehouses переведено на табси. Інвентаризація у всіх табсах. Залишок беклогу і підготовка до міграції.
## Поточні задачі (Sprint)

- [x] 2026-06-23: Інтеграція скрипту списання в FastAPI (`POST /api/admin/write-off`), cron о 22:00, очистка БД рецептів, GT серія, МВ рецепти, чохли М/В, n8n виправлено.
- [x] 2026-06-24: Система поповнення `cases_components` з конвертацією одиниць, три акордеони алярмів, флоу фурнітури (алярм → Матей → водій → БД), `ReplenishModal` як спільний компонент.
- [x] G19 — виправити рецепт (преміальні шампури замість звичайних) — простий SQL.
- [x] Колонка "Різниця" у складах — замінена повноцінною інвентаризацією.
- [x] Підсвітка `min_limit` в акордеонах складів.
- [x] Демо-режим (Login.tsx, api.ts) — вимкнено.
- [x] 2026-06-25: Нова логіка write-off, фурнітура в AdminTasker, sticky header, підсвітка min_limit.
- [x] 2026-06-26: Інвентаризація складів (inventory_checks), логування комплектух, history_logs переробка, GT серія в БД, AdminSystem — логи списань.
- [ ] FIFO по партіях — архітектура узгоджена, код не написаний.
- [ ] Підготовка до міграції на новий сервер.
- [x] 2026-06-29: loot_box архітектура — нові таблиці `loot_box_operative` / `loot_box_main`, 53 позиції ящиків перенесено, write_off автовизначає таблицю, GET /api/stock +2 категорії, AdminWarehouses табс "Ящики", AdminDashboard акордеон "Ящики", ClipboardCheck у всіх табсах для всіх колонок.
## Проблеми

- Структура БД `tid` як PK є "артефактом", але поки залишається для сумісності з `production_log`.
- ⚠️ **УВАГА ДЛЯ АГЕНТІВ (Засвоєний урок):** Будь-який монолітний перезапис файлів (таких як `src/components/UI.tsx` або `src/pages/MasterCabinet.tsx`) має відбуватися виключно на основі оригінальних сирників. Скорочення коду, видалення невикористовуваних на поточній сторінці компонентів або вирізання сусідніх вкладок призводить до повної сплячки інтерфейсу або падіння збірки.

---

## На горизонті (наступні спринти)

- **Вкладка Опт (наступний спринт):** замінює "Відправки" в кабінеті адміна.
  Бронювання компонентів з трьох таблиць при плануванні оптових грилів.
  Списання по тригеру ТТН з н8н. Окреме TG повідомлення для опту.
  Архітектура повністю узгоджена. Деталі — Issues_Log.md.

---
## [2026-06-29] Сесія 16: loot_box архітектура — COMPLETED

- **БД:** нові таблиці `bot_workshop.loot_box_operative` (структура як inventory_operative) і `bot_workshop.loot_box_main` (структура як inventory_main). Перенесено 53 позиції компонентів ящиків (Турист, BBQ, Віскі, Винний, Лазня, Автолюбитель макс/стандарт, Полуничка, Пивний). Додано 4 нові з qty=0: Костер х4, Декантер, Камені, Щипці. Позиції видалено зі старих таблиць.
- **`run_write_off`:** перед UPDATE компонента перевіряє наявність `item_id` в `loot_box_operative` — якщо є, списує звідти; якщо ні — з `inventory_operative`. `history_logs.source` динамічний.
- **`GET /api/stock`:** +2 UNION ALL блоки для `loot_box_main` і `loot_box_operative` з category відповідно.
- **`GET /api/notifications`:** алярми з `loot_box_operative` і `loot_box_main` потрапляють в загальний список.
- **`PATCH /api/admin/inventory`** і **`POST /api/admin/inventory/check`:** підтримують нові `table_key`: `loot_box_operative`, `loot_box_main`.
- **`AdminWarehouses.tsx`:** переведено з акордеонів на табси (`Склади / Грилі / Кейс-компоненти / Ящики`). Новий табс "Ящики" — `LootBoxRow` інтерфейс, `lootBoxMap`, `startEditLootBox`, `saveEditLootBox`.
- **`AdminDashboard.tsx`:** 4-й акордеон "Ящики" в секції алярмів, `lootBoxAlerts` фільтр, `formatSource` +2 кейси.
- **Інвентаризація:** ClipboardCheck додано у всі табси для всіх колонок з відповідними `table_key`.
- **⚠️ Важливо:** тригер `trg_operative_replenish` на `loot_box_operative` ще не створено — алярми поповнення майстерні для ящиків не працюють. Наступна задача.
## [2026-06-26] Сесія 14: Інвентаризація + UX складів — COMPLETED

- **`AdminWarehouses.tsx`:** видалено Δ-колонки (були реалізовані в Fix #14, але замінені повноцінною інвентаризацією). Sticky header в акордеонах через `max-height` + `overflow-y: auto` на контейнері (оновлено з попереднього підходу).
- **БД:** нова таблиця `bot_workshop.inventory_checks` (id, item_id, table_key, system_qty, actual_qty, delta, checked_at, note) + індекс по (item_id, table_key).
- **`POST /api/admin/inventory/check`:** читає поточний qty з відповідної таблиці, рахує delta, пише в inventory_checks.
- **`GET /api/admin/inventory/checks/latest`:** DISTINCT ON (item_id, table_key) — останній запис для кожної позиції.
- **`InventoryCheckModal.tsx`:** новий компонент — велике поле вводу (inputMode=numeric для мобайлу), live-preview delta, createPortal в document.body, підтримка від'ємних фактичних значень. ⚠️ **Кнопка "Прийняти фактичне значення" (оновити qty в системі) — ще не реалізована, наступна ітерація.**
- **`AdminWarehouses.tsx`:** Δ-бейдж під назвою товару (`+N ↑` / `−N ↓`), іконка ClipboardCheck для запуску інвентаризації поруч з олівцем.
- **Архітектура інвентаризації:** інвентаризація ≠ ручне коригування. Ручний олівець — свідомі операції Матея. Інвентаризація — фіксує реальний факт і показує розбіжність. Кнопка "Прийняти фактичне значення" — наступна ітерація.
- **history_logs переробка:** стара структура (item_id, change_qty, operation_type) замінена на нову (session_id, article, component, qty, source, operation). Таблицю дропнуто і створено заново. Старі дані не переносились — були тестові.
- **Логування комплектух:** в `run_write_off` додано `INSERT INTO history_logs` для кожної комплектухи в циклі рецептів (`write_off_component`). Тепер видно що саме списалось з `inventory_operative`.
- **session_id:** генерується один раз на початку `run_write_off` через `uuid.uuid4()` — групує всі записи одного запуску.
- **GT серія в БД:** додано G1T/G1TH, G6T/G6TH, G8T/G8TH, G10T/G10TH, G12T/G12TH в `inventory_cases`, `inventory_finished`, `inventory_finished_main`.
- **AdminSystem — Логи списань:** нова вкладка в адмінці. Табси (Логи / Рецепти грилів / Рецепти кейсів). Фільтри по даті і операції. Акордеони по session_id — заголовок показує дату+час і унікальні артикули сесії. Таблиця: Артикул / Компонент / Кількість / Операція. Кольорові бейджі з українськими назвами операцій. Стиль — відповідає дизайн-системі проєкту.
- **Новий ендпоінт:** `GET /api/admin/writeoff-logs` з фільтрами `date_from`, `date_to`, `operation`, `search`.

## [2026-06-25] Fix #14: Переробка логіки вечірнього списання + UI правки — COMPLETED

- **Логіка write-off:** нова пріоритетність списання — `inventory_finished` → `inventory_cases` + `recipes` → `inventory_operative` (кейс) або `recipes` → `inventory_operative` (не-кейс). Видалено блок `recipes_cases`/`cases_components` з `run_write_off` — вони оновлюються при логуванні майстра (`/api/master/logs`), не при списанні. Протестовано через dry_run: всі три сценарії (finished покриває повністю, частково, не покриває) працюють коректно. Файл: `bbq-api/main.py`, функція `run_write_off`.
- **AdminTasker — фурнітура:** другий дропдаун (cases_components, is_internal=false) з конвертацією одиниць і live-підказкою `= X шт`. Тоггл Основний/Майстерня, дефолт — Основний. Новий ендпоінт `GET /api/items/components`.
- **AdminWarehouses — кнопка олівця:** видалена з акордеону Кейс-компоненти. Залишився тільки ➕ (ReplenishModal).
- **AdminWarehouses — sticky header:** перший підхід — `position: sticky; top: 0`, `overflow-hidden` видалено (замінено в Сесії 14 на `max-height` + `overflow-y: auto`).
- **AdminWarehouses — колонка Δ:** додано дві незалежні пари (різниця + дата) для колонок Оперативний і Основний. Дані локальні, без API. _(Замінено в Сесії 14 повноцінною `inventory_checks`.)_
- **AdminWarehouses — підсвітка min_limit:** червона активна (`quantity < min_limit`), жовта поки неактивна (потребує `max_limit` з API).
- **api.ts — демо-режим:** видалено `.catch(() => console.log('demo mode'))` з `confirmTasker`.

## [2026-06-24] Сесія 13: Cases Components — конвертація, алярми, флоу фурнітури — COMPLETED

- **Перевірка write-off скрипту:** підтверджено що подвійне списання 23.06 було виключно через ручний тестовий запуск вранці + cron о 22:00 на ті самі записи. В штатному режимі (тільки cron) повторення неможливе — всі записи позначаються `is_written_off=true` після кожного запуску.
- **Балончики GT:** додано в `recipes_cases` для G1T, G6T, G8T, G10T, G12T і їх H-версій (`items_per_case=0.17`).
- **`cases_components` — нові колонки БД:** `unit_type VARCHAR(20)`, `conversion_factor NUMERIC(10,4)`, `is_internal BOOLEAN`. Шурупи=kg/25000, резинка=roll/50, ремінець=roll/100, липучка=roll/25, ручка=strip/7. Перегородки/віяло/цицьки/П-шка/фіксатори → `is_internal=true`, решта → `false`.
- **`incoming_tasks` — нові колонки БД:** `component_id` (FK→cases_components), `input_qty`, `unit_type`, `conversion_factor` — для тасок фурнітури.
- **Новий компонент `ReplenishModal.tsx`:** єдина модалка поповнення з конвертацією (кг/мотки/полоски/шт), live-підказка `= X шт/м`, опціональний тоггл складу (Основний/Майстерня).
- **`POST /api/admin/components/{id}/replenish`:** розгалуження — якщо `is_internal=true` → одразу UPDATE cases_components + history_log; якщо `is_internal=false` і є `warehouse` → створює таску водію в `incoming_tasks` зі статусом `'очікується'`.
- **`GET /api/driver/tasks`:** повертає нові поля `component_id, input_qty, unit_type, conversion_factor`.
- **`POST /api/driver/tasks/{id}/deliver`:** якщо таска має `component_id` → UPDATE cases_components; якщо ні → стара логіка inventory_main/operative.
- **`AdminWarehouses.tsx`:** акордеон Кейс-компоненти — підсвітка червоним якщо `qty < min_threshold`, кількість в природних одиницях (кг/м/шт), кнопки ➕ (ReplenishModal) і ✏️ (пряме редагування).
- **`AdminDashboard.tsx`:** три акордеони алярмів — Склад / Фурнітура / Внутрішні; закриті за замовчуванням, сірі якщо 0 алярмів, лічильник на кожному. Секція "Зарплатний фонд майстрів" видалена повністю.
- **`Tasker.tsx`:** картка фурнітури показує `input_qty + одиниця` (напр. "5 кг"), поля фасування приховані, вибір складу залишається.

## [2026-06-23] Сесія Fix #13: Інтеграція скрипту списання + очистка БД рецептів — COMPLETED

- `write_off_service.py` не був в crontab — місяць не виконувався. Проведено повний аудит, виправлено критичні баги в БД та логіці.
- `POST /api/admin/write-off?dry_run=false/true` — FastAPI ендпоінт з повною логікою списання. Cron: `0 22 * * 1-5`.
- Детальний лог — Issues_Log.md, Fix #13.

## [2026-06-22] Сесія 12: Логістичний блок — розумні одиниці + "Замовити" з алярму — COMPLETED

- Розумні одиниці у картці водія, нова колонка `pcs_per_box`, три сценарії фасування, повноцінна модалка "Замовити" з алярму.

## [2026-06-20] Сесія UI/UX правок + знахідка Telegram-бота — COMPLETED

- UI/UX фікси адмінки, кабінет водія допрацьовано, виявлено і задокументовано систему Telegram-бота поповнення складу.